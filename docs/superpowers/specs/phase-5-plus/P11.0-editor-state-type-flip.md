# P11.0 — EditorState type flip + document-construction migration

**Subject:** Flip `EditorState.state` field from `StateNode` to the new `State` type. Migrate document-construction (`createEmptyDocument`, `createInitialEditorState`). Keep the existing render call alive via a bridge so the old render pipeline still runs. Do NOT touch `EditorState.selection` (still legacy Position-by-path; selection migration is P11.3) and do NOT migrate any action handlers (those are P11.1+).

**Reference:** Strategy doc Decision 4 + round-1/round-4 fixes; master spec migration step 10b prereq.

## Goal

Action handlers in P11.1+ require `EditorState.state` to already be the new `State` type. P11.0 lands that prerequisite type flip in one focused phase before family-by-family handler migration begins. After P11.0, `EditorState.state` holds the Yjs-backed `State` (per decision C) and any caller using `editorState.state` gets the new type.

The render call in `createInitialEditorState` continues to consume `stateLegacy` (the canonical record during the parallel window, per decision D). P11.0 carries both representations: `state: State` (type-flip target; derived from legacy via `rebuildStateFromLegacy` on every legacy action) and `stateLegacy: StateNode` (canonical). Render-pipeline rewiring (cutover to consume `state`) is the responsibility of cutover (after P7 + P11.4).

History wrapper introduction (decision C, refined by decision D point 9) is bundled into P11.0: `EditorState.history: EditorHistory` flips to `History` (the wrapper type). During the parallel window, the wrapper delegates to a retained `historyLegacy: EditorHistory` field — undo/redo remains user-functional throughout. At cutover, `historyLegacy` is deleted and the wrapper switches to delegating to Y.UndoManager. Action handlers in P11.1+ call `pushHistoryEntry` (which the wrapper routes to whichever backend is active).

## Dependencies

P4e (Yjs rebase — `State` runs on Yjs) AND P6 (`State.embedContents` introduced) — needed for the new `State` shape that the type flip targets. P11.0 does NOT need P9 (no action handlers calling cursor ops yet) or P7/P8 (no render rewire).

## Current state

- `/Users/hansyu/code/taleweaver/packages/core/src/editor/editor-state.ts`. Imports `StateNode` from `../state/state-node`, `Selection` from `../cursor/selection`, `Change` from `../state/change`. The `EditorState` interface holds `state: StateNode` (legacy) plus selection plus history.
- `createInitialEditorState` (in editor-state.ts) calls `createEmptyDocument()` (legacy factory in `state/initial-state.ts`), then `renderTree(state, registry)`, then `cascadePass`, then `layoutTree`. The pipeline is: state → render → cascade → layout.
- `state/new-initial-state.ts` exists already from Phase 1 — produces a new-shape `State`. (May be incomplete; verify.)
- `Change` type wraps `StateNode` via `Action` & `prevState` fields. The `EditorHistory` mechanism intertwines `Change` with selection metadata. Per decision C + decision D point 9, `EditorHistory` is renamed to `historyLegacy` and continues backing undo through the parallel window; a new `History` wrapper provides the consumer-facing API. The wrapper delegates to `historyLegacy` during parallel, to Y.UndoManager after cutover. `Change` itself stays in place during the parallel window (it's the implementation detail of `historyLegacy`).

## Files involved

**Modified:**
- `editor/editor-state.ts` — flip `state: StateNode` → `state: State`. Update constructors. Bridge or replace the renderTree call (decide in plan).
- Whichever document-construction path is currently used (`createEmptyDocument` from legacy or `new-initial-state.ts` from Phase 1) — pick the new one and route through it.
- `state/change.ts` — likely needs updating since `Change` wraps legacy state. Decide whether to migrate `Change` here or defer.
- `state/history.ts` — possibly needs reconciliation with the editor's `EditorHistory`.

**Resolved — see `decisions.md` decision D (2026-05-16, revised):** Option B (dual representation) with **rebuild-based sync**, not diff-replay. `stateLegacy` is the canonical record during the parallel window; `state` is the type-flip target derived from it. Legacy actions mutate `stateLegacy`; `state` rebuilt via `rebuildStateFromLegacy` (with BlockIds derived from path via `pathToBlockId` so selection survives). Migrated actions (P11.1+) mutate `state`; `stateLegacy` derived via `downgradeToStateNode`. Renderer continues consuming `stateLegacy` until cutover (P7 + handler migration done). `stateLegacy` and `historyLegacy` deleted at cutover.

## Key technical considerations

1. **`createEmptyDocument`.** Legacy version returns `StateNode`. New version (`createEmptyDocumentNew` or whatever from `new-initial-state.ts`) returns `State`. P11.0 routes through the new version. May need to export it from index.ts.

2. **`Change` and history.** Legacy `Change` wraps StateNode. Two paths:
   - (a) Migrate `Change` to wrap `State` in P11.0 (touches editor-state.ts, change.ts, history.ts, all undo/redo handlers).
   - (b) Defer `Change` migration to P11.x action family migrations (each handler migrates its own change-creation).
   - Probably (a) for atomicity — flip `EditorState.state` AND `EditorState.history` consistently. But this risks scope creep. Plan decides.

3. **Selection field NOT touched.** Per round-4 strategy fix: `EditorState.selection` stays on legacy `Position`-by-path. Action handlers in P11.1/P11.2 will read selection (legacy type) and produce new `State` mutations. The cursor module continues to handle selection via legacy types until P11.3 selection action family migrates it.

4. **TypeScript will catch every mismatch.** Once `EditorState.state` flips, every consumer that uses `editorState.state` needs updating. That's a LOT of consumers — but most can be temporarily satisfied by passing through to legacy code via the bridge. The plan should expect 20-30+ TypeScript errors when the flip happens, and methodically resolve them.

5. **Test pass criterion.** After P11.0, the editor still functions end-to-end (open document, see something rendered). Action handlers still work via legacy paths. Selection still works via legacy paths. Only the `EditorState.state` field's type and document-construction has changed.

## Risks and patterns to apply

- **TypeScript scoping discipline.** When flipping a load-bearing type, do it in a tight commit so the breakage window is minimal. Resolve all errors in one focused effort. Don't batch unrelated changes.
- **Browser smoke critical.** Pixels could break in many ways during this flip. Smoke-test before declaring done.
- **Implementer escalation:** if any consumer of `editorState.state` requires a substantive migration that's not "use the bridge to convert to legacy" — STOP and escalate. Substantive migration belongs to P11.x.

## Test strategy

P11.0 is structural. Tests focus on:
- `EditorState.state` is the new `State` type (TypeScript verifies).
- `createInitialEditorState` produces an editor with a valid `State` containing initial blocks.
- The bridge (if any) correctly converts `State` → `StateNode` for legacy callers.
- All existing editor tests still pass (action handlers operate via legacy paths; selection unchanged).
- Browser smoke: open the example app, see the empty document render correctly.

## Open questions

1. ✅ **Bridge mechanism — resolved in `decisions.md` decision D (2026-05-16).** Dual representation with rebuild-based sync; see "Open" section above.
2. ✅ **`Change` migration timing — resolved by decisions C + D.** `Change` retired AT CUTOVER (not P11.0). During the parallel window, `Change` continues to back legacy `historyLegacy` (which is still user-functional). At cutover, `historyLegacy` is deleted; `Change` becomes dead code; the `History` wrapper switches to Y.UndoManager. Migrated P11.x handlers call `pushHistoryEntry` (which the wrapper routes to whichever backend is active).
3. ✅ **`EditorHistory` collapse — resolved by decisions C + D.** Renamed to `historyLegacy` in P11.0 (per decision E symbol-naming clause). `EditorState.history: EditorHistory` flips to `History` (wrapper); wrapper delegates to `historyLegacy` during parallel, to Y.UndoManager at cutover. `historyLegacy` deleted at cutover.
4. ✅ **`state/initial-state.ts` rename — resolved by decision E.** P11.0 Task 1 renames `state/initial-state.ts` → `state/initial-state-legacy.ts` and `state/new-initial-state.ts` → `state/initial-state.ts`. Build stays green throughout (only paths change). Dual-rep init happens in subsequent tasks.

## Task ordering for per-phase plan

To preserve a green build through the type flip, the per-phase plan MUST sequence tasks as:

1. **Task 1: rename existing factories per decision E.** `state/initial-state.ts` → `state/initial-state-legacy.ts`; `state/new-initial-state.ts` → `state/initial-state.ts`. Update all imports. No logic change; build stays green; this is a pure rename commit.
2. **Task 2: rename `EditorHistory` → `historyLegacy` on `EditorState`.** Update consumers. No logic change.
3. **Task 3: introduce `History` wrapper type and `pushHistoryEntry` indirection.** Wrapper delegates to `historyLegacy` initially (this is its only backend at P11.0). Consumers go through the wrapper.
4. **Task 4: introduce `state: State` field on `EditorState`** populated from `createEmptyDocument()` (new factory). Initially `state` is just constructed at init; not updated by action handlers yet.
5. **Task 5: wire `rebuildStateFromLegacy(stateLegacy): State`** to fire after every action that mutates `stateLegacy`. Implement `pathToBlockId`-based BlockId derivation. Round-trip equivalence tests per decision D point 10. The `pathToBlockId` helper lands in `state/path-to-block-id.ts` (a small standalone util that survives cutover for any consumer that still wants path-encoded ids).
6. **Task 6: create `editor/legacy-position-bridge.ts`** with all four conversion helpers — `legacyPositionToNew`, `newPositionToLegacy`, `legacySelectionToNew`, `newSelectionToLegacy` — per decision D point 6. Tests for each direction. The whole file is transitional, deleted at cutover (the legacy → new direction earlier in P11.3, the new → legacy direction at cutover end of P11.4).
7. **Task 7: browser smoke** — verify the editor still works end-to-end. Type, click, undo, redo.

## Success criteria

- `EditorState.state: State` (new type, derived from `stateLegacy` via `rebuildStateFromLegacy`).
- `EditorState.stateLegacy: StateNode` (canonical during parallel window) is also present and updated on every action.
- `createInitialEditorState` produces a valid editor with both representations populated and in agreement.
- Browser smoke: editor renders the empty document via legacy renderer reading `stateLegacy`. Typing characters works. Undo/redo works (via `historyLegacy` per decision D point 9).
- All existing tests still pass — including undo/redo tests, since `historyLegacy` continues backing undo during the parallel window. The wrapper `History` interface is the only consumer-facing change.
- Round-trip equivalence: for at least a representative subset of Layer 3 ops, apply legacy op → rebuild new state → state shape matches a direct new-state construction of the same scenario (per decision D point 10).
- Build green.

## Review cycle expectations

Pre-execution: yes. Post-execution: yes. Browser smoke is non-negotiable for this phase.

## Estimated commits

~5-7.
