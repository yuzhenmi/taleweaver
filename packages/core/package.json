{
  "name": "@taleweaver/core",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck:headless": "tsc -p tsconfig.headless.json",
    "lint:boundaries": "depcruise src --config .dependency-cruiser.cjs"
  },
  "dependencies": {
    "yjs": "~13.6.18"
  },
  "devDependencies": {
    "@taleweaver/print": "0.0.1",
    "dependency-cruiser": "^17.4.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
