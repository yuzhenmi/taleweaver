import TaleWeaver from '../TaleWeaver';
import State from './State';

export default function parseStateJSON(taleWeaver: TaleWeaver, stateJSON: object): State {
  const state = new State();
  // TODO: Construct state from stateJSON
  return state;
}
